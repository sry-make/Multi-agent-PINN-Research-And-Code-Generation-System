# ─────────────────────────────────────────────────────────────
# PINN Research Agent V2 — Python 依赖清单
# 安装: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# ── 编排框架 ────────────────────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=1.0.0            # 核心类型（BaseMessage / Document 等均在此包，1.x API）
langchain-community>=0.3.0
langchain-openai>=0.2.0          # Ollama 兼容 OpenAI 接口

# ── LLM 客户端 ──────────────────────────────────────────────
openai>=1.30.0                   # Ollama /v1 兼容层

# ── RAG / 向量库 ────────────────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0     # bge-m3 + bge-reranker-base

# ── PDF 解析 ────────────────────────────────────────────────
pypdf>=4.0.0

# ── 外部搜索工具 ────────────────────────────────────────────
arxiv>=2.1.0                     # arXiv Python SDK
duckduckgo-search>=6.0.0         # 免费网络搜索（SerpAPI 备选）

# ── 公式推导 ────────────────────────────────────────────────
sympy>=1.12

# ── TUI ─────────────────────────────────────────────────────
textual>=0.70.0                  # 终端 UI 框架（类 kimi-cli）
rich>=13.7.0                     # 富文本渲染（Textual 依赖）

# ── 代码沙盒 ────────────────────────────────────────────────
docker>=7.0.0                    # Python Docker SDK

# ── 可观测性 ────────────────────────────────────────────────
langsmith>=0.1.0                 # 可选：LangSmith 云端 Tracing

# ── 科学计算（沙盒内运行，也在宿主机安装便于测试）───────────
numpy>=1.26.0
matplotlib>=3.8.0
scipy>=1.12.0
torch>=2.2.0                     # PINN 代码通常依赖 torch

# ── 工具库 ──────────────────────────────────────────────────
pydantic>=2.6.0                  # 状态 Schema 定义
python-dotenv>=1.0.0             # .env 环境变量加载
tiktoken>=0.7.0                  # Token 计数（成本控制）
httpx>=0.27.0                    # 异步 HTTP 客户端
tenacity>=8.3.0                  # 重试装饰器
